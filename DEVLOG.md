# やること

- Sin動くようにして
- NARMAを動かして
- OUNを動かしたい タイムプレックスはきついので できればなしがいい
- 4:2でqubit分けるノイズ
- 比率の正規化
- シミュ：6qubitでxyz全部はかってるけど → 実際にはxはかる6qubit、yはかる6qubit、zはかる6qubitを用意することになるでしょう

- DividedForwardを使うかSingleForwardを使うかによって結果が異なるTODOを解決したい
    - t=0に近いところでは差がないのではないか
      [ ] 視覚的に確認するため、PredictionResultをwashoutせずに、washoutしているところとしていないところのマスクを取得できるようにする
      [ ] washoutしていないところで学習する
      [ ] washoutしていないところでスコアを算出する（score.py使用箇所）
      [ ] preview.pyではwashoutしている部分も灰色に描画する。

# プロンプト

## 実装・修正時の注意点

- 物理系やスコア関数など、主要クラス・関数には日本語docstringを付与すること。
- ユーザーが指示した修正は、例やメリットの説明は不要で、直接コードに反映すること。
- 型安全性・可読性・保守性を重視すること。
- エラー処理やエラーメッセージは、従来のものを引き継ぐこと。
- ユーザーがx_arr, y_arrなどの命名規則や、リスト型には要素型を明示するなどのコーディングスタイルを好むこと。
- クラス内変数はprotectedで宣言することを好む。
- PyQtのparent引数はOptionalではなくparent: QObject = Noneとする。
- テストでは配列の値は明示的に書くこと。
- Python 3.10+を前提とし、typing genericsはできるだけ使わない。
- 可視化でプロット点が重なる問題があれば、バイオリンプロットなど分布を明確に表示する手法を採用すること。
- プロットでは平均値・中央値を表示して統計的特徴を明確化すること。
- 実行時間やメモリ使用量を考慮して、適切なサンプル数やパラメータ数に調整すること。
- 関数やメソッドの命名では、その目的や処理内容が明確に分かるようにすること。

## 他の作業でも注意すべき点

- APIや関数の分割・統合・命名規則の一貫性を重視すること。
- プロジェクト構成やimportパスの変更時は、全体の整合性を必ず保つこと。
- ユーザーのコーディングスタイル・型注釈・命名規則・docstring方針を守ること。
- ユーザーの指示があった場合は、説明や例は省略し、即座にコードへ反映すること。
- 型安全性・エラー処理・可読性を常に意識すること。
- API変更時は、呼び出し側も必ず修正すること。
- ライブラリのバージョン固有の問題（qutip 5.2のcallback非対応など）に遭遇した場合は、代替手法で同等機能を実現すること。
- 既存クラスに新しいメソッドを追加する場合は、一貫性のあるAPI設計を心がけること。

## 命名規則

### 配列変数の命名規則

配列変数の命名では、ある時刻のデータそのものが持つ要素数を`n`、時刻の数を`seq`
とし、snake_caseでshapeの順に並べる。

#### 基本形式

- `変数名` : スカラー（単一時刻・単一要素）
- `変数名_seq` : 1次元配列（shape: (seq,) = (T,)）時系列データ
- `変数名_seq_n` : 2次元配列（shape: (seq, n) = (T, n_dim)）多次元時系列データ

#### データ種別ごとの命名

- **時刻データ**: `t_seq` : 時刻配列 (T,)
- **入力データ**: `u_seq_n` : 入力配列 (T, n_in)
- **出力データ**: `y_seq_n` : 出力配列 (T, n_out)
- **状態データ**: `x_seq_n` : 状態配列 (T, n_state)

#### 予測・真値データ

予測結果や真値を扱う場合は、`y_true_xx`, `y_pred_xx`の形式を使用する。

- `y_true_seq_n` : 真値の出力配列 (T, n_out)
- `y_pred_seq_n` : 予測値の出力配列 (T, n_out)

一般的なy配列（予測・真値の区別が不要な場合）は`true`, `pred`を省略する。

#### 従来表記からの変更例

- `t_arr` → `t_seq`
- `u_mlt_arr` → `u_seq_n`
- `y_mlt_arr` → `y_seq_n`
- `states` → `x_seq_n`

#### 避けるべき表記

`y_seq`, `y_t`などの曖昧な表記は使用しない。

---

# 作業ログ

## 2025年8月12日

### time_evol_solver_v2の機能拡張と量子測定実装

- **主要成果**: 時間発展中の状態を取り出すことで、期待値の他に誤差（分散）やサンプルも取得可能に
- **核心機能**: `time_evol_solver_v2.py`が自走し始め、量子状態の完全な統計的解析が可能になった
- **実装内容**:
    - `StepResult`クラスで密度行列（`rho`）ベースの状態管理を実現
    - `expect()`, `variance()`, `std()`, `sample()`メソッドによる包括的な量子測定機能
    - `sample_with_post_rho()`による測定後状態の取得
    - `TimeEvolutionResult`からの各時刻状態へのアクセス機能
- **意義**: 従来の期待値のみの制限から脱却し、量子測定の完全なシミュレーションが実現

### バイオリンプロットによる可視化改善

- **問題**: `main_nv_sensor.py`の`_test_samples`関数で散布図のプロット点が重なってしまい、分布が見にくい状況
- **解決策**: 散布図をバイオリンプロットに変更して、各B値に対するサンプル分布を明確に可視化
- **実装内容**:
    - `_test_samples`関数をバイオリンプロット形式に変更
    - `matplotlib.pyplot.violinplot`を使用して分布形状を可視化
    - 平均値（赤線）と中央値（オレンジ線）を表示
    - サンプル数を効率化のため100個に調整、B値の点数を15個に減少

### qutip 5.2対応とコールバック機能実装

- **問題**: qutip 5.2では`callback`オプションが存在しないため、`time_evol_solver_v2.py`でエラーが発生
- **解決策**: `e_ops`に関数を渡すことでコールバック機能を実現
- **実装内容**:
    - `QutipMESolveTimeEvolutionSolver.forward`メソッドの修正
    - `options`から`"callback": store_state_callback`を削除
    - `e_ops=[store_state_callback]`として、従来のコールバック機能を維持
    - `result_index`が`None`の場合のアサーション問題も修正

### TimeEvolutionResultの機能拡張

- **追加機能**: `TimeEvolutionResult`クラスに`sample_measurements`メソッドが追加
- **機能概要**: 最後の時刻での観測値サンプリングを簡素化
- **使用方法**: `result.sample_measurements(obs_qobj, n_samples=n_samples, rng=rng)`
- **効果**: `main_nv_sensor.py`での観測値サンプリングコードが簡潔化