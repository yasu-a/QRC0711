# やること

- Sin動くようにして
- NARMAを動かして
- OUNを動かしたい タイムプレックスはきついので できればなしがいい
- 4:2でqubit分けるノイズ
- 比率の正規化
- シミュ：6qubitでxyz全部はかってるけど → 実際にはxはかる6qubit、yはかる6qubit、zはかる6qubitを用意することになるでしょう

- DividedForwardを使うかSingleForwardを使うかによって結果が異なるTODOを解決したい
    - t=0に近いところでは差がないのではないか
      [ ] 視覚的に確認するため、PredictionResultをwashoutせずに、washoutしているところとしていないところのマスクを取得できるようにする
      [ ] washoutしていないところで学習する
      [ ] washoutしていないところでスコアを算出する（score.py使用箇所）
      [ ] preview.pyではwashoutしている部分も灰色に描画する。

# プロンプト

## 実装・修正時の注意点

- 物理系やスコア関数など、主要クラス・関数には日本語docstringを付与すること。
- ユーザーが指示した修正は、例やメリットの説明は不要で、直接コードに反映すること。
- 型安全性・可読性・保守性を重視すること。
- エラー処理やエラーメッセージは、従来のものを引き継ぐこと。
- ユーザーがx_arr, y_arrなどの命名規則や、リスト型には要素型を明示するなどのコーディングスタイルを好むこと。
- クラス内変数はprotectedで宣言することを好む。
- PyQtのparent引数はOptionalではなくparent: QObject = Noneとする。
- テストでは配列の値は明示的に書くこと。
- Python 3.10+を前提とし、typing genericsはできるだけ使わない。

## 他の作業でも注意すべき点

- APIや関数の分割・統合・命名規則の一貫性を重視すること。
- プロジェクト構成やimportパスの変更時は、全体の整合性を必ず保つこと。
- ユーザーのコーディングスタイル・型注釈・命名規則・docstring方針を守ること。
- ユーザーの指示があった場合は、説明や例は省略し、即座にコードへ反映すること。
- 型安全性・エラー処理・可読性を常に意識すること。
- API変更時は、呼び出し側も必ず修正すること。

## 命名規則

### 配列変数の命名規則

配列変数の命名では、ある時刻のデータそのものが持つ要素数を`n`、時刻の数を`seq`
とし、snake_caseでshapeの順に並べる。

#### 基本形式

- `変数名` : スカラー（単一時刻・単一要素）
- `変数名_seq` : 1次元配列（shape: (seq,) = (T,)）時系列データ
- `変数名_seq_n` : 2次元配列（shape: (seq, n) = (T, n_dim)）多次元時系列データ

#### データ種別ごとの命名

- **時刻データ**: `t_seq` : 時刻配列 (T,)
- **入力データ**: `u_seq_n` : 入力配列 (T, n_in)
- **出力データ**: `y_seq_n` : 出力配列 (T, n_out)
- **状態データ**: `x_seq_n` : 状態配列 (T, n_state)

#### 予測・真値データ

予測結果や真値を扱う場合は、`y_true_xx`, `y_pred_xx`の形式を使用する。

- `y_true_seq_n` : 真値の出力配列 (T, n_out)
- `y_pred_seq_n` : 予測値の出力配列 (T, n_out)

一般的なy配列（予測・真値の区別が不要な場合）は`true`, `pred`を省略する。

#### 従来表記からの変更例

- `t_arr` → `t_seq`
- `u_mlt_arr` → `u_seq_n`
- `y_mlt_arr` → `y_seq_n`
- `states` → `x_seq_n`

#### 避けるべき表記

`y_seq`, `y_t`などの曖昧な表記は使用しない。